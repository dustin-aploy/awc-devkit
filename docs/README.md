# AWC Devkit Docs

This folder contains the main onboarding and workflow guides for `awc-devkit`.

Start here:

- `self-hosted-onboarding.md` for the end-to-end local setup path
- `run-a-self-hosted-worker.md` for running a worker with the reference runtime
- `validate-a-worker.md` for schema, runtime, and test-suite checks

Use `../examples/README.md` alongside these docs if you want concrete contracts, tasks, and sample outputs.
